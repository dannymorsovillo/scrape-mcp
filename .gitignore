node_modules/
dist/
*.log
.DS_Store
.claude

# Written per-machine by server/dist/install.js — holds absolute paths.
.mcp.json

# Datasets collected by walk_endpoint — real scraped rows, not source.
/output/
